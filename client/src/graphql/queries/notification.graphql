subscription NotificationSubscription($userId: uuid!) {
    notifications(
        where: { user_id: { _eq: $userId } }
        order_by: { created_at: desc }
        limit: 20
    ) {
        id
        type
        title
        message
        data
        is_read
        created_at
    }
}
